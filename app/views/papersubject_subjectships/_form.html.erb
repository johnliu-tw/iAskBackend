<%= form_for(papersubject_subjectship) do |f| %>
  <% if papersubject_subjectship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(papersubject_subjectship.errors.count, "error") %> prohibited this papersubject_subjectship from being saved:</h2>

      <ul>
      <% papersubject_subjectship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
