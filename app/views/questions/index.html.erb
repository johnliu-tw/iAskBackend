<p id="notice"><%= notice %></p>

<h1>Questions</h1>

<table>
  <thead>
    <tr>
      <th>題號</th>
      <th>題幹</th>
      <th>題型</th>
      <th>Answer</th>
      <th>Analysis</th>
      <th>Analysis att</th>
      <th>Analysis url</th>
      <th>Question type</th>
      <th>Active</th>
      <th>Optioncount</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @questions.each do |question| %>
      <tr>
        <td><%= question.order %></td>     
        <td><%= question.title %></td>
        <td><%= question.question_type %></td>
        <td><%= question.answer %></td>
        <td><%= question.analysis %></td>
        <td><%= question.analysis_att %></td>
        <td><%= question.analysis_url %></td>
        <td><%= question.question_type %></td>
        <td><%= question.active %></td>
        <td><%= question.optionCount %></td>
        <td><%= link_to 'Show', question %></td>
        <td><%= link_to 'Edit', edit_question_path(question) %></td>
        <td><%= link_to 'Destroy', question, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Selection Question', new_paper_question_path %>
