<%= simple_form_for(@grade) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, label: "年級名稱" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "送出", class: "btn btn-primary" %>
  </div>
<% end %>
